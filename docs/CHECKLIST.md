# ✅ ЧЕКЛИСТ - Исправление регистрации

## 🎯 Цель
Исправить проблему с регистрацией в приложении video-marketplace-app

---

## 📋 Шаг 1: Обновить конфигурацию Firebase

### 1.1 Обновить google-services.json
- [ ] Найти файл `google-services.json` в Downloads (уже скачан)
- [ ] Скопировать в проект: `android/app/google-services.json`
- [ ] Заменить старый файл новым
- [ ] Проверить, что файл находится в правильной папке

### 1.2 Проверить package name
- [ ] Открыть `android/app/build.gradle`
- [ ] Найти строку `applicationId`
- [ ] Убедиться, что значение: `"com.example.tiktok_tutorial"`
- [ ] Если не совпадает - исправить

---

## 📋 Шаг 2: Проверить разрешения

### 2.1 Проверить AndroidManifest.xml
- [ ] Открыть `android/app/src/main/AndroidManifest.xml`
- [ ] Найти секцию `<manifest>`
- [ ] Убедиться, что есть строки:
  ```xml
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
  ```
- [ ] Если нет - добавить перед `<application>`

---

## 📋 Шаг 3: Добавить логирование

### 3.1 Найти файл регистрации
- [ ] Найти файл с кодом регистрации (обычно `sign_up_screen.dart` или `register_screen.dart`)
- [ ] Открыть файл в редакторе

### 3.2 Добавить логи
- [ ] Добавить `print('🔵 Starting registration...')` в начале функции
- [ ] Добавить `print('✅ Success!')` после успешной регистрации
- [ ] Добавить `print('❌ Error: $e')` в блоке catch

### 3.3 Пример кода
```dart
Future<void> registerUser() async {
  print('🔵 Starting registration...');
  print('Email: ${emailController.text}');
  
  try {
    print('🔵 Creating user in Firebase Auth...');
    UserCredential userCredential = await FirebaseAuth.instance
        .createUserWithEmailAndPassword(
      email: emailController.text.trim(),
      password: passwordController.text.trim(),
    );
    print('✅ User created: ${userCredential.user!.uid}');

    print('🔵 Creating user profile in Firestore...');
    await FirebaseFirestore.instance
        .collection('users')
        .doc(userCredential.user!.uid)
        .set({
      'email': emailController.text.trim(),
      'createdAt': FieldValue.serverTimestamp(),
    });
    print('✅ Profile created successfully!');

    // Показать успешное сообщение
    ScaffoldMessenger.of(context).showSnackBar(
      SnackBar(content: Text('Регистрация успешна!')),
    );

  } on FirebaseAuthException catch (e) {
    print('❌ Firebase Auth Error: ${e.code} - ${e.message}');
    
    // Показать ошибку пользователю
    String errorMessage = 'Ошибка регистрации';
    if (e.code == 'weak-password') {
      errorMessage = 'Пароль слишком слабый';
    } else if (e.code == 'email-already-in-use') {
      errorMessage = 'Email уже используется';
    } else if (e.code == 'invalid-email') {
      errorMessage = 'Неправильный формат email';
    }
    
    ScaffoldMessenger.of(context).showSnackBar(
      SnackBar(content: Text(errorMessage)),
    );
    
  } catch (e) {
    print('❌ Unexpected error: $e');
    ScaffoldMessenger.of(context).showSnackBar(
      SnackBar(content: Text('Неожиданная ошибка: $e')),
    );
  }
}
```

- [ ] Скопировать и адаптировать код для своего проекта

---

## 📋 Шаг 4: Проверить инициализацию Firebase

### 4.1 Проверить main.dart
- [ ] Открыть `lib/main.dart`
- [ ] Убедиться, что есть инициализация Firebase:
  ```dart
  void main() async {
    WidgetsFlutterBinding.ensureInitialized();
    await Firebase.initializeApp();
    runApp(MyApp());
  }
  ```
- [ ] Если нет - добавить

---

## 📋 Шаг 5: Пересобрать APK

### 5.1 Очистить кэш
- [ ] Открыть терминал в папке проекта
- [ ] Выполнить: `flutter clean`
- [ ] Дождаться завершения

### 5.2 Собрать APK
- [ ] Выполнить: `flutter build apk --release`
- [ ] Дождаться завершения сборки
- [ ] Найти APK в `build/app/outputs/flutter-apk/app-release.apk`

---

## 📋 Шаг 6: Тестирование

### 6.1 Установить APK
- [ ] Скопировать APK на устройство
- [ ] Установить APK
- [ ] Открыть приложение

### 6.2 Подключить логи
- [ ] Подключить устройство к компьютеру через USB
- [ ] Включить USB debugging на устройстве
- [ ] Выполнить: `adb devices` (проверить, что устройство видно)
- [ ] Выполнить: `adb logcat | grep -i firebase`

### 6.3 Попробовать зарегистрироваться
- [ ] Ввести email (например, `test2@example.com`)
- [ ] Ввести пароль (минимум 6 символов)
- [ ] Нажать кнопку регистрации
- [ ] Посмотреть логи в терминале
- [ ] Проверить, появилось ли сообщение об успехе или ошибке

### 6.4 Проверить Firebase Console
- [ ] Открыть https://console.firebase.google.com/project/video-marketplace-app-53a6d/authentication/users
- [ ] Обновить страницу
- [ ] Проверить, появился ли новый пользователь в списке

---

## 📋 Шаг 7: Проверка результата

### Если регистрация успешна ✅
- [ ] Пользователь появился в Firebase Console
- [ ] Приложение показало сообщение об успехе
- [ ] Пользователь перенаправлен на главный экран
- [ ] **ГОТОВО! Проблема решена! 🎉**

### Если регистрация не работает ❌
- [ ] Посмотреть логи в терминале
- [ ] Найти строки с ❌ или Error
- [ ] Скопировать текст ошибки
- [ ] Прочитать debugging_guide.md для дополнительной помощи

---

## 🧪 Тестовые данные

### Существующий пользователь (для проверки входа)
- Email: `test@example.com`
- Password: `Test123456`

### Для тестирования регистрации
Использовать новые email:
- `test2@example.com`
- `test3@example.com`
- `myemail@example.com`

---

## 📁 Полезные файлы

- [ ] `firebase_diagnostic_report.md` - Подробный отчет
- [ ] `debugging_guide.md` - Инструкция по отладке
- [ ] `problem_analysis.md` - Анализ проблемы
- [ ] `recommended_firestore_rules.txt` - Улучшенные правила (опционально)
- [ ] `google-services.json` - Актуальная конфигурация (в Downloads)

---

## 🆘 Если ничего не помогло

1. Прочитать `debugging_guide.md`
2. Проверить все пункты этого чеклиста еще раз
3. Убедиться, что устройство имеет доступ к интернету
4. Попробовать собрать debug APK: `flutter build apk --debug`
5. Проверить, что Firebase инициализирован в main.dart

---

## ✅ Финальная проверка

- [ ] google-services.json обновлен
- [ ] AndroidManifest.xml содержит интернет-разрешения
- [ ] Код регистрации содержит try-catch
- [ ] Ошибки показываются пользователю
- [ ] Firebase инициализирован в main.dart
- [ ] APK пересобран
- [ ] Тестирование выполнено с логами
- [ ] Регистрация работает!

---

**Удачи! 🚀**

**Firebase готов к работе, осталось только исправить код приложения!**
