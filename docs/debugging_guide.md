# 🐛 Краткая инструкция по отладке регистрации

## Быстрая проверка

### 1. Проверить логи приложения

**Подключить устройство к компьютеру и запустить:**

```bash
# Для Android
adb logcat | grep -E "(firebase|auth|firestore)"

# Или более подробно
adb logcat *:E
```

**Попробовать зарегистрироваться и посмотреть, какие ошибки появляются.**

---

### 2. Проверить интернет-разрешения

**Файл: `android/app/src/main/AndroidManifest.xml`**

Убедиться, что есть эти строки:

```xml
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
```

---

### 3. Проверить google-services.json

**Файл: `android/app/google-services.json`**

1. Скачать актуальный файл из Firebase Console (уже скачан в Downloads)
2. Заменить старый файл новым
3. Проверить, что package_name совпадает с applicationId в build.gradle

---

### 4. Добавить логирование в код

**Найти файл регистрации и добавить print/Log:**

```dart
// Flutter
print('🔵 Starting registration...');
print('Email: ${emailController.text}');

try {
  print('🔵 Creating user...');
  UserCredential userCredential = await FirebaseAuth.instance
      .createUserWithEmailAndPassword(
    email: emailController.text.trim(),
    password: passwordController.text.trim(),
  );
  print('✅ User created: ${userCredential.user!.uid}');
  
} catch (e) {
  print('❌ Error: $e');
  // ВАЖНО: Показать ошибку пользователю!
  ScaffoldMessenger.of(context).showSnackBar(
    SnackBar(content: Text('Ошибка: $e')),
  );
}
```

---

### 5. Пересобрать APK

```bash
# Очистить кэш
flutter clean

# Собрать новый APK
flutter build apk --release

# Или для отладки
flutter build apk --debug
```

---

### 6. Тестовый пользователь

**Для проверки входа можно использовать:**
- Email: `test@example.com`
- Password: `Test123456`

Этот пользователь уже создан в Firebase Console.

---

## Частые проблемы

### Проблема 1: "Ничего не происходит"
**Причина:** Нет обработки ошибок в коде
**Решение:** Добавить try-catch и показывать ошибки пользователю

### Проблема 2: "Network error"
**Причина:** Нет интернет-разрешений
**Решение:** Добавить разрешения в AndroidManifest.xml

### Проблема 3: "Invalid API key"
**Причина:** Устаревший google-services.json
**Решение:** Обновить файл из Firebase Console

### Проблема 4: "User not found in Firestore"
**Причина:** Профиль не создается после регистрации
**Решение:** Проверить код создания профиля в Firestore

---

## Контрольный список

- [ ] Проверил логи через adb logcat
- [ ] Добавил интернет-разрешения в AndroidManifest.xml
- [ ] Обновил google-services.json
- [ ] Добавил логирование в код регистрации
- [ ] Добавил обработку ошибок (try-catch)
- [ ] Показываю ошибки пользователю (SnackBar/Toast)
- [ ] Пересобрал APK
- [ ] Протестировал на устройстве с включенным logcat

---

## Полезные команды

```bash
# Посмотреть подключенные устройства
adb devices

# Установить APK
adb install -r app-release.apk

# Очистить данные приложения
adb shell pm clear com.example.tiktok_tutorial

# Посмотреть логи Firebase
adb logcat | grep -i firebase

# Посмотреть только ошибки
adb logcat *:E
```

---

## Если ничего не помогло

1. Проверить Firebase Console - появляются ли новые пользователи?
2. Попробовать зарегистрироваться с другим email
3. Проверить, работает ли интернет на устройстве
4. Попробовать собрать debug APK вместо release
5. Проверить, инициализирован ли Firebase в main.dart

---

**Удачи в отладке! 🚀**
