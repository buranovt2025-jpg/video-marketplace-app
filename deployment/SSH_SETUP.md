# 🔐 Инструкция по настройке SSH для автоматического деплоя

Этот документ описывает процесс настройки SSH подключения к серверу Digital Ocean без пароля для безопасного и автоматического деплоя GoGoMarket.

---

## 📚 Содержание

1. [Предварительные требования](#предварительные-требования)
2. [Создание SSH ключей](#создание-ssh-ключей)
3. [Добавление ключа на сервер Digital Ocean](#добавление-ключа-на-сервер)
4. [Настройка SSH Config](#настройка-ssh-config)
5. [Тестирование подключения](#тестирование-подключения)
6. [Решение проблем](#решение-проблем)

---

## 📦 Предварительные требования

Перед началом убедитесь, что у вас есть:

- ✅ Локальный компьютер с Linux, macOS или Windows (WSL)
- ✅ Установленный SSH клиент (`ssh` и `ssh-keygen`)
- ✅ Доступ к серверу Digital Ocean:
  - IP адрес: `165.232.81.31`
  - Пользователь: `root`
  - Пароль: **[REDACTED]**

> ⚠️ **ВАЖНО:**
> - **Никогда не храните пароли/ключи в репозитории.**
> - Если пароль когда-либо попадал в чат/репозиторий/скриншоты — **считайте его скомпрометированным и смените**.
> - После настройки SSH ключей рекомендуется отключить вход по паролю для повышения безопасности.

---

## 🔑 Создание SSH ключей

SSH ключи позволяют авторизовываться на сервере без ввода пароля. Это более безопасно и удобно для автоматизации.

### Шаг 1: Проверьте наличие существующих ключей

Откройте терминал и выполните:

```bash
ls -la ~/.ssh/
```

Если вы видите файлы `id_rsa` и `id_rsa.pub` (или `id_ed25519` и `id_ed25519.pub`), значит у вас уже есть SSH ключи. Можете перейти к [Шагу 3](#шаг-3-скопируйте-публичный-ключ).

### Шаг 2: Генерация новой пары ключей

Если ключей нет, создадим новые:

#### **Вариант 1: RSA (традиционный, широко поддерживаемый)**

```bash
ssh-keygen -t rsa -b 4096 -C "your_email@example.com"
```

#### **Вариант 2: Ed25519 (современный, более безопасный, рекомендуется)**

```bash
ssh-keygen -t ed25519 -C "your_email@example.com"
```

При генерации вы увидите:

```
Generating public/private ed25519 key pair.
Enter file in which to save the key (/home/user/.ssh/id_ed25519):
```

**Нажмите Enter**, чтобы сохранить ключ в стандартном месте.

Затем вам предложат установить passphrase (пароль для ключа):

```
Enter passphrase (empty for no passphrase):
Enter same passphrase again:
```

> 💡 **Рекомендация:** Для автоматического деплоя оставьте пустым (нажмите Enter дважды). Если хотите дополнительную безопасность, установите пароль, но тогда придется вводить его при каждом деплое.

### Шаг 3: Скопируйте публичный ключ

После генерации выведите содержимое публичного ключа:

```bash
# Для RSA:
cat ~/.ssh/id_rsa.pub

# Для Ed25519:
cat ~/.ssh/id_ed25519.pub
```

Скопируйте весь вывод (начинается с `ssh-rsa` или `ssh-ed25519`).

Пример:

```
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIAbCdEfG... your_email@example.com
```

---

## 📤 Добавление ключа на сервер Digital Ocean

Теперь нужно добавить публичный ключ на сервер, чтобы он распознавал ваш компьютер.

### Метод 1: Использование `ssh-copy-id` (рекомендуется)

Это самый простой способ:

```bash
ssh-copy-id root@165.232.81.31
```

Вы увидите запрос на ввод пароля:

```
root@165.232.81.31's password:
```

Введите пароль: **[REDACTED]**

После успешного добавления вы увидите:

```
Number of key(s) added: 1

Now try logging into the machine, with:   "ssh 'root@165.232.81.31'"
and check to make sure that only the key(s) you wanted were added.
```

✅ **Готово!** Теперь можете подключаться к серверу без пароля!

---

### Метод 2: Ручное добавление ключа

Если `ssh-copy-id` не работает, можно добавить ключ вручную:

#### Шаг 1: Подключитесь к серверу по SSH

```bash
ssh root@165.232.81.31
```

Введите пароль: **[REDACTED]**

#### Шаг 2: Создайте директорию `.ssh` (если ее нет)

```bash
mkdir -p ~/.ssh
chmod 700 ~/.ssh
```

#### Шаг 3: Добавьте публичный ключ в `authorized_keys`

```bash
echo "YOUR_PUBLIC_KEY" >> ~/.ssh/authorized_keys
```

Замените `YOUR_PUBLIC_KEY` на скопированный публичный ключ.

#### Шаг 4: Установите правильные права доступа

```bash
chmod 600 ~/.ssh/authorized_keys
```

#### Шаг 5: Выйдите из сервера

```bash
exit
```

---

## ⚙️ Настройка SSH Config

Для удобства можно создать алиас для подключения к серверу.

### Шаг 1: Откройте или создайте файл `~/.ssh/config`

```bash
nano ~/.ssh/config
```

### Шаг 2: Добавьте конфигурацию для GoGoMarket сервера

Добавьте следующие строки:

```ssh-config
Host gogomarket
    HostName 165.232.81.31
    User root
    Port 22
    IdentityFile ~/.ssh/id_ed25519
    # Или ~/.ssh/id_rsa, если используете RSA
    ServerAliveInterval 60
    ServerAliveCountMax 3
```

### Шаг 3: Сохраните и закройте файл

- Для nano: нажмите `Ctrl+X`, затем `Y`, затем `Enter`
- Для vim: нажмите `Esc`, затем `:wq`, затем `Enter`

### Шаг 4: Установите правильные права для конфига

```bash
chmod 600 ~/.ssh/config
```

### Шаг 5: Теперь можно подключаться просто:

```bash
ssh gogomarket
```

🎉 **Намного проще, чем писать `ssh root@165.232.81.31` каждый раз!**

---

## ✅ Тестирование подключения

### 1. Простое подключение

Попробуйте подключиться к серверу:

```bash
# Если настроили SSH config:
ssh gogomarket

# Или напрямую:
ssh root@165.232.81.31
```

Если все настроено правильно, вы подключитесь **без запроса пароля**.

### 2. Проверка с командой

Проверьте, что команды выполняются удаленно:

```bash
ssh gogomarket "echo 'Hello from server!'"
```

Если вы увидите `Hello from server!` — все работает!

### 3. Проверка скриптом

Запустите скрипт проверки деплоя:

```bash
cd ~/cursor_deploy_automation
./check_deployment.sh
```

Если все проверки прошли успешно — готово к автоматическому деплою!

---

## 🔧 Решение проблем

### Проблема 1: «Продолжает запрашивать пароль»

**Причины:**

1. Ключ не добавлен на сервер
2. Неправильные права доступа к файлам

**Решение:**

1. Проверьте, что ключ добавлен на сервере:

```bash
ssh root@165.232.81.31 "cat ~/.ssh/authorized_keys"
```

Вы должны увидеть свой публичный ключ.

2. Проверьте права доступа на сервере:

```bash
ssh root@165.232.81.31 "ls -la ~/.ssh"
```

Права должны быть:
- `.ssh/` → `700` (drwx------)
- `.ssh/authorized_keys` → `600` (-rw-------)

3. Исправьте права, если необходимо:

```bash
ssh root@165.232.81.31 "chmod 700 ~/.ssh && chmod 600 ~/.ssh/authorized_keys"
```

---

### Проблема 2: «Permission denied (publickey)»

**Причина:** SSH не может найти или использовать ваш приватный ключ.

**Решение:**

1. Укажите ключ явно:

```bash
ssh -i ~/.ssh/id_ed25519 root@165.232.81.31
```

2. Добавьте ключ в SSH agent:

```bash
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519
```

3. Проверьте, что ключ добавлен:

```bash
ssh-add -l
```

---

### Проблема 3: «Connection timed out»

**Причины:**

1. Сервер не запущен
2. Firewall блокирует SSH (порт 22)
3. Неправильный IP адрес

**Решение:**

1. Проверьте доступность сервера:

```bash
ping 165.232.81.31
```

2. Проверьте, открыт ли порт SSH:

```bash
nc -zv 165.232.81.31 22
```

3. Проверьте firewall на Digital Ocean:
   - Зайдите в панель Digital Ocean
   - Networking → Firewalls
   - Убедитесь, что порт 22 открыт для вашего IP

---

### Проблема 4: «WARNING: REMOTE HOST IDENTIFICATION HAS CHANGED»

**Причина:** Сервер был переустановлен или изменен SSH ключ сервера.

**Решение:**

Удалите старую запись из `known_hosts`:

```bash
ssh-keygen -R 165.232.81.31
```

Затем попробуйте подключиться снова:

```bash
ssh root@165.232.81.31
```

---

## 🔒 Дополнительная безопасность (опционально)

После настройки SSH ключей рекомендуется отключить вход по паролю.

### Шаг 1: Подключитесь к серверу

```bash
ssh root@165.232.81.31
```

### Шаг 2: Откройте конфигурацию SSH

```bash
sudo nano /etc/ssh/sshd_config
```

### Шаг 3: Найдите и измените следующие строки:

```
PasswordAuthentication no
PermitRootLogin prohibit-password
```

### Шаг 4: Сохраните и перезапустите SSH

```bash
sudo systemctl restart sshd
```

> ⚠️ **ОСТОРОЖНО:** Перед выполнением этих шагов убедитесь, что SSH ключи работают! Иначе вы можете потерять доступ к серверу.

---

## 🎉 Готово!

Теперь вы можете:

✅ Подключаться к серверу без пароля
✅ Использовать автоматический деплой через `./auto_deploy.sh`
✅ Запускать Cursor для автоматических исправлений и деплоев

Для дальнейшей работы смотрите:
- [README.md](./README.md) — Общая инструкция
- [CURSOR_INSTRUCTIONS.md](./CURSOR_INSTRUCTIONS.md) — Инструкции для Cursor AI

---

**Если у вас остались вопросы или проблемы, смотрите раздел Troubleshooting в [README.md](./README.md).**
