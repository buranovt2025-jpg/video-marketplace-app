# 🤖 Инструкции для Cursor AI / Composer

Это детальное руководство для Cursor AI (через Composer или Agent) по автоматическому исправлению ошибок и деплою приложения GoGoMarket на Digital Ocean.

---

## 📋 Содержание

1. [Обзор проекта](#обзор-проекта)
2. [Ваша роль как AI ассистента](#ваша-роль-как-ai-ассистента)
3. [Рабочий процесс](#рабочий-процесс)
4. [Анализ ошибок](#анализ-ошибок)
5. [Исправление ошибок](#исправление-ошибок)
6. [Тестирование исправлений](#тестирование-исправлений)
7. [Коммит изменений](#коммит-изменений)
8. [Запуск деплоя](#запуск-деплоя)
9. [Проверка деплоя](#проверка-деплоя)
10. [Обработка ошибок деплоя](#обработка-ошибок-деплоя)
11. [Примеры сценариев](#примеры-сценариев)
12. [Ограничения и меры предосторожности](#ограничения-и-меры-предосторожности)

---

## 🎯 Обзор проекта

### Информация о проекте

**Название:** GoGoMarket
**Тип:** Flutter Web Application (Marketplace с видео-рилсами)
**Репозиторий:** buranovt2025-jpg/video-marketplace-app
**Основная ветка для деплоя:** main
**Сервер:** Digital Ocean Droplet
**IP адрес:** 165.232.81.31
**URL:** https://165.232.81.31

### Архитектура приложения

- **Frontend:** Flutter Web (canvaskit renderer)
- **Backend:** Firebase (предположительно)
- **Web Server:** Nginx
- **Деплой:** Автоматический через скрипты в `~/cursor_deploy_automation/`

### Основные функции приложения

1. Регистрация и авторизация пользователей (продавцы, покупатели, курьеры)
2. Создание товаров продавцами
3. Создание видео-рилсов для продвижения товаров
4. Каталог и поиск товаров
5. Корзина покупок
6. Оформление заказов
7. Система доставки

### Известные критические проблемы

⚠️ **Проблемы, требующие вашего внимания:**

1. **Невозможно завершить оформление заказа** (Приоритет: КРИТИЧЕСКИЙ)
   - После нажатия "Заказать" страница зависает
   - Заказ не создается в базе данных
   - Отсутствует обработка ошибок

2. **Страница "Заказы" не загружается** (Приоритет: КРИТИЧЕСКИЙ)
   - Пустая страница при переходе на раздел заказов
   - Проблема у всех типов пользователей

3. **Видео-рилсы не публикуются** (Приоритет: ВЫСОКИЙ)
   - После создания рилса нет подтверждения
   - Рилсы не появляются в ленте

4. **Изображения товаров не загружаются** (Приоритет: СРЕДНИЙ)
   - Изображения по URL не отображаются
   - Показываются только placeholder иконки

---

## 🤖 Ваша роль как AI ассистента

Вы — AI ассистент разработчика, который должен:

1. ✅ **Анализировать код** и находить причины ошибок
2. ✅ **Исправлять ошибки** в коде Flutter/Dart
3. ✅ **Тестировать исправления** локально
4. ✅ **Коммитить изменения** с понятными сообщениями
5. ✅ **Запускать автоматический деплой** на сервер
6. ✅ **Проверять результат** деплоя
7. ✅ **Отчитываться** о выполненной работе

❌ **Чего НЕ нужно делать:**

- Изменять критические настройки сервера без подтверждения
- Удалять файлы конфигурации
- Вносить изменения в production базу данных напрямую
- Деплоить непротестированный код
- Игнорировать ошибки тестирования

---

## 🔄 Рабочий процесс

### Полный цикл исправления и деплоя

```
1. Получение задачи от пользователя
   ↓
2. Анализ кода и поиск ошибок
   ↓
3. Исправление ошибок
   ↓
4. Локальное тестирование (flutter run -d chrome)
   ↓
5. Коммит изменений в git
   ↓
6. Push в репозиторий
   ↓
7. Запуск автоматического деплоя
   ↓
8. Проверка успешности деплоя
   ↓
9. Отчет пользователю
```

---

## 🔍 Анализ ошибок

### Шаг 1: Понимание проблемы

Когда пользователь сообщает об ошибке, задайте уточняющие вопросы:

```
🤖: "Я понял проблему: [краткое описание].
    Для точного исправления мне нужна дополнительная информация:
    
    1. Какие действия приводят к ошибке?
    2. Есть ли сообщение об ошибке в консоли?
    3. Проблема возникает у всех пользователей или определенных типов?
    4. Когда проблема появилась впервые?"
```

### Шаг 2: Поиск файлов с ошибкой

Используйте команду поиска для нахождения релевантных файлов:

```bash
# Поиск по ключевым словам
grep -r "функция_с_ошибкой" lib/

# Поиск файлов, связанных с заказами
find lib/ -name "*order*.dart"

# Просмотр структуры проекта
tree lib/ -L 3
```

### Шаг 3: Анализ кода

Откройте найденные файлы и проанализируйте:

1. **Логику функций**
2. **Обработку ошибок** (try-catch блоки)
3. **API вызовы** (правильность endpoints)
4. **Состояние приложения** (state management)
5. **Валидацию данных**

### Пример анализа

```
🤖: "Я проанализировал код файла lib/features/orders/screens/checkout_screen.dart
    
    Обнаружены следующие проблемы:
    
    1. ❌ В функции _submitOrder() отсутствует try-catch блок
    2. ❌ Нет проверки успешности создания заказа
    3. ❌ Отсутствует навигация после создания заказа
    4. ❌ Не обрабатывается случай пустого ответа от API
    
    Предлагаю следующие исправления: [...]"
```

---

## 🛠️ Исправление ошибок

### Общие принципы исправления

1. **Минимальные изменения** — исправляйте только то, что сломано
2. **Обработка ошибок** — всегда добавляйте try-catch блоки
3. **Валидация данных** — проверяйте входные данные
4. **Пользовательский feedback** — показывайте статус операций
5. **Логирование** — добавляйте print/log для отладки

### Пример исправления критической ошибки

**Проблема:** Заказ не создается

**Было (плохо):**

```dart
Future<void> _submitOrder() async {
  final order = Order(
    items: cartItems,
    totalPrice: totalPrice,
  );
  
  await orderService.createOrder(order);
  // Нет обработки ошибок, нет навигации
}
```

**Стало (хорошо):**

```dart
Future<void> _submitOrder() async {
  // Показываем индикатор загрузки
  setState(() => _isLoading = true);
  
  try {
    // Создаем заказ
    final order = Order(
      items: cartItems,
      totalPrice: totalPrice,
      customerAddress: _addressController.text,
      deliveryTariff: _selectedTariff,
      paymentMethod: _selectedPaymentMethod,
    );
    
    // Отправляем на сервер
    final createdOrder = await orderService.createOrder(order);
    
    // Проверяем успешность
    if (createdOrder != null && createdOrder.id != null) {
      // Скрываем индикатор
      setState(() => _isLoading = false);
      
      // Показываем успешное сообщение
      ScaffoldMessenger.of(context).showSnackBar(
        SnackBar(
          content: Text('Заказ #${createdOrder.id} создан успешно!'),
          backgroundColor: Colors.green,
        ),
      );
      
      // Переходим на страницу подтверждения
      Navigator.of(context).pushReplacement(
        MaterialPageRoute(
          builder: (_) => OrderConfirmationScreen(order: createdOrder),
        ),
      );
    } else {
      throw Exception('Заказ не был создан на сервере');
    }
  } catch (e) {
    // Обрабатываем ошибку
    setState(() => _isLoading = false);
    
    print('Ошибка создания заказа: $e');
    
    // Показываем ошибку пользователю
    ScaffoldMessenger.of(context).showSnackBar(
      SnackBar(
        content: Text('Ошибка создания заказа: ${e.toString()}'),
        backgroundColor: Colors.red,
        action: SnackBarAction(
          label: 'Повторить',
          onPressed: _submitOrder,
        ),
      ),
    );
  }
}
```

### Типичные шаблоны исправлений

#### 1. Добавление try-catch блока

```dart
try {
  // Рискованный код
  await riskyOperation();
} catch (e) {
  print('Ошибка: $e');
  // Показать пользователю
  showErrorDialog(context, e.toString());
}
```

#### 2. Добавление валидации

```dart
if (_formKey.currentState!.validate()) {
  // Выполняем действие только если форма валидна
  await submitForm();
} else {
  // Показываем ошибки валидации
  ScaffoldMessenger.of(context).showSnackBar(
    SnackBar(content: Text('Пожалуйста, заполните все поля')),
  );
}
```

#### 3. Исправление навигации

```dart
// Вместо push (может накопиться стек)
Navigator.of(context).push(...);

// Используйте pushReplacement для замены текущей страницы
Navigator.of(context).pushReplacement(...);

// Или popAndPushNamed для очистки стека
Navigator.of(context).popAndPushNamed('/home');
```

#### 4. Добавление индикаторов загрузки

```dart
if (_isLoading) {
  return Center(child: CircularProgressIndicator());
}

// Или в кнопке
ElevatedButton(
  onPressed: _isLoading ? null : _submitOrder,
  child: _isLoading 
    ? SizedBox(
        width: 20, 
        height: 20, 
        child: CircularProgressIndicator(color: Colors.white)
      )
    : Text('Заказать'),
)
```

---

## ✅ Тестирование исправлений

### Шаг 1: Локальный запуск приложения

```bash
# Перейдите в директорию проекта
cd /home/ubuntu/video-marketplace-app

# Запустите приложение в браузере
flutter run -d chrome --web-renderer canvaskit
```

### Шаг 2: Проверка исправленного функционала

Воспроизведите шаги, которые ранее вызывали ошибку:

**Пример для тестирования заказов:**

```
✅ 1. Авторизуйтесь как покупатель (customer1@gogomarket.com / Customer123!)
✅ 2. Добавьте товар в корзину
✅ 3. Перейдите в корзину
✅ 4. Нажмите "Оформить заказ"
✅ 5. Заполните форму:
      - Адрес доставки
      - Комментарий
      - Тариф доставки
      - Способ оплаты
✅ 6. Нажмите "Заказать"
✅ 7. Проверьте:
      ❓ Появилась ли страница подтверждения?
      ❓ Создан ли заказ в базе данных?
      ❓ Отображается ли заказ в разделе "Заказы"?
```

### Шаг 3: Проверка консоли на ошибки

Откройте DevTools в браузере (F12) и проверьте вкладку Console:

```
✅ Нет красных ошибок
⚠️  Есть предупреждения (желтые) — приемлемо
❌ Есть ошибки (красные) — нужно исправить
```

### Шаг 4: Отчет о тестировании

```
🤖: "Тестирование завершено:

    ✅ Исправление работает корректно
    ✅ Заказ создается успешно
    ✅ Отображается страница подтверждения
    ✅ Заказ виден в разделе 'Заказы'
    ✅ Нет ошибок в консоли
    
    Готов к коммиту и деплою."
```

---

## 📝 Коммит изменений

### Правила написания коммитов

Используйте **conventional commits** формат:

```
<тип>(<область>): <краткое описание>

<опциональное подробное описание>

<опциональные футеры>
```

### Типы коммитов

- `fix:` — исправление бага
- `feat:` — новая функция
- `docs:` — изменения в документации
- `style:` — форматирование кода
- `refactor:` — рефакторинг без изменения функционала
- `test:` — добавление тестов
- `chore:` — изменения в конфигурации

### Примеры хороших коммитов

```bash
# Пример 1: Исправление критической ошибки
git commit -m "fix(orders): исправлена проблема создания заказа

- Добавлен try-catch блок в _submitOrder()
- Добавлена навигация на страницу подтверждения
- Добавлена проверка успешности создания заказа
- Добавлены уведомления для пользователя

Fixes #1 - Невозможно завершить оформление заказа"

# Пример 2: Исправление загрузки страницы
git commit -m "fix(orders): исправлена загрузка страницы заказов

- Добавлена обработка пустого списка заказов
- Исправлен Future builder
- Добавлено сообщение 'У вас пока нет заказов'

Fixes #2 - Страница заказов не загружается"

# Пример 3: Улучшение UX
git commit -m "feat(orders): добавлены индикаторы загрузки

- Индикатор при создании заказа
- Индикатор при загрузке списка заказов
- Улучшена обратная связь с пользователем"
```

### Команды для коммита

```bash
# Проверка измененных файлов
git status

# Добавление всех измененных файлов
git add .

# Или добавление конкретных файлов
git add lib/features/orders/screens/checkout_screen.dart

# Коммит с сообщением
git commit -m "fix(orders): исправлена проблема создания заказа"

# Push в удаленный репозиторий
git push origin main
```

---

## 🚀 Запуск деплоя

### Автоматический деплой через скрипт

После успешного коммита и push запустите автоматический деплой:

```bash
# Перейдите в директорию со скриптами
cd ~/cursor_deploy_automation

# Запустите скрипт деплоя
./auto_deploy.sh
```

### Что происходит во время деплоя

Скрипт автоматически выполняет следующие шаги:

```
1. ✅ Проверка окружения (Flutter, Git, SSH)
2. ✅ Проверка SSH подключения к серверу
3. ✅ Проверка новых изменений в Git
4. ✅ Обновление кода (git pull)
5. ✅ Сборка Flutter web приложения
6. ✅ Создание резервной копии текущей версии
7. ✅ Копирование файлов на сервер
8. ✅ Перезапуск nginx
9. ✅ Проверка доступности сайта
```

### Мониторинг деплоя

Во время деплоя следите за выводом скрипта:

```
✅ Зеленый текст — успешный шаг
⚠️  Желтый текст — предупреждение или информация
❌ Красный текст — ошибка (деплой прерван)
```

### Время выполнения

Обычный деплой занимает **3-5 минут**:

- Сборка приложения: 2-3 минуты
- Копирование файлов: 30 секунд
- Перезапуск nginx: 5 секунд

---

## 🔍 Проверка деплоя

### Автоматическая проверка

После завершения деплоя автоматически запускается проверка:

```bash
# Или запустите вручную
./check_deployment.sh
```

### Ручная проверка

1. **Откройте сайт в браузере:**

```
https://165.232.81.31
```

2. **Проверьте исправленный функционал:**

Воспроизведите шаги, которые ранее вызывали ошибку, чтобы убедиться, что проблема решена.

3. **Проверьте консоль браузера:**

Откройте DevTools (F12) и убедитесь, что нет ошибок.

4. **Проверьте логи nginx на сервере (при необходимости):**

```bash
ssh gogomarket "tail -f /var/log/nginx/error.log"
```

### Отчет об успешном деплое

```
🤖: "✅ Деплой завершен успешно!

    📊 Результаты проверки:
    ✅ Сайт доступен (HTTP 200)
    ✅ Nginx работает корректно
    ✅ Все файлы загружены на сервер
    ✅ Исправленный функционал работает
    
    🔗 Приложение доступно: https://165.232.81.31
    📝 Логи деплоя: ~/cursor_deploy_automation/logs/deploy_YYYYMMDD_HHMMSS.log
    
    Рекомендую протестировать основные функции приложения."
```

---

## ❌ Обработка ошибок деплоя

### Типичные ошибки и их решения

#### Ошибка 1: SSH подключение не удалось

**Симптомы:**

```
❌ Не удалось подключиться к серверу!
```

**Решение:**

```bash
# Проверьте SSH ключ
ssh-add -l

# Если ключ не добавлен, добавьте его
ssh-add ~/.ssh/id_ed25519

# Протестируйте подключение
ssh gogomarket "echo 'Тест подключения'"
```

#### Ошибка 2: Flutter build failed

**Симптомы:**

```
❌ Ошибка при сборке приложения!
```

**Решение:**

```bash
# Очистите кэш и пересоберите
flutter clean
flutter pub get
flutter build web --release --web-renderer canvaskit
```

#### Ошибка 3: Nginx не запускается

**Симптомы:**

```
❌ Ошибка при перезапуске nginx!
```

**Решение:**

```bash
# Подключитесь к серверу
ssh gogomarket

# Проверьте конфигурацию nginx
sudo nginx -t

# Просмотрите логи nginx
sudo tail -f /var/log/nginx/error.log

# Перезапустите nginx вручную
sudo systemctl restart nginx
```

#### Ошибка 4: Сайт возвращает 500 ошибку

**Симптомы:**

```
⚠️  Сайт вернул код 500
```

**Решение:**

```bash
# Подключитесь к серверу
ssh gogomarket

# Проверьте логи nginx
sudo tail -f /var/log/nginx/error.log

# Проверьте права доступа к файлам
ls -la /var/www/html

# Исправьте права, если необходимо
sudo chmod -R 755 /var/www/html
```

### Откат к предыдущей версии

Если деплой завершился с ошибкой и сайт не работает:

```bash
# Найдите последнюю резервную копию
ls -lt ~/cursor_deploy_automation/backups/

# Подключитесь к серверу
ssh gogomarket

# Восстановите из резервной копии
# (замените XXXXXX на имя файла из backups/)
cd /var/www/html
sudo rm -rf *
sudo tar -xzf /tmp/backup_XXXXXX.tar.gz

# Перезапустите nginx
sudo systemctl restart nginx
```

---

## 📚 Примеры сценариев

### Сценарий 1: Исправление критической ошибки заказов

**Пользователь:** "Заказ не создается, страница зависает"

**Ваши действия:**

```
🤖: "Я понял проблему. Начинаю анализ кода заказов..."

[Анализ]
1. Открываю lib/features/orders/screens/checkout_screen.dart
2. Обнаруживаю отсутствие обработки ошибок в _submitOrder()
3. Обнаруживаю отсутствие навигации после создания заказа

[Исправление]
1. Добавляю try-catch блок
2. Добавляю проверку успешности создания
3. Добавляю навигацию на страницу подтверждения
4. Добавляю индикатор загрузки

[Тестирование]
1. Запускаю flutter run -d chrome
2. Авторизуюсь как покупатель
3. Создаю тестовый заказ
4. ✅ Заказ создается успешно
5. ✅ Отображается страница подтверждения

[Коммит]
git commit -m "fix(orders): исправлена проблема создания заказа"
git push origin main

[Деплой]
cd ~/cursor_deploy_automation
./auto_deploy.sh

[Проверка]
./check_deployment.sh

✅ Деплой завершен успешно!
✅ Функционал заказов восстановлен
🔗 https://165.232.81.31
```

### Сценарий 2: Добавление новой функции

**Пользователь:** "Добавь кнопку 'Купить в один клик' на странице товара"

**Ваши действия:**

```
🤖: "Добавляю функцию быстрой покупки..."

[Анализ]
1. Нахожу lib/features/products/screens/product_detail_screen.dart
2. Изучаю текущую логику добавления в корзину
3. Планирую реализацию быстрой покупки

[Реализация]
1. Создаю функцию _quickBuy() 
2. Добавляю кнопку рядом с "В корзину"
3. Реализую логику:
   - Добавление товара в корзину
   - Немедленный переход на страницу оформления
4. Добавляю обработку ошибок

[Тестирование]
1. flutter run -d chrome
2. Открываю страницу товара
3. ✅ Кнопка отображается корректно
4. ✅ Нажатие переводит на оформление заказа
5. ✅ Товар автоматически в корзине

[Коммит]
git commit -m "feat(products): добавлена кнопка 'Купить в один клик'"
git push origin main

[Деплой]
./auto_deploy.sh

✅ Новая функция успешно добавлена и задеплоена!
```

---

## ⚠️ Ограничения и меры предосторожности

### Что можно делать свободно

✅ Исправлять ошибки в коде
✅ Добавлять обработку ошибок
✅ Улучшать UX (индикаторы загрузки, уведомления)
✅ Рефакторить код для читаемости
✅ Добавлять логирование для отладки
✅ Запускать тесты
✅ Коммитить и пушить код
✅ Запускать автоматический деплой
✅ Проверять результаты деплоя

### Что требует подтверждения пользователя

⚠️ Изменение структуры базы данных
⚠️ Удаление файлов конфигурации
⚠️ Изменение критических настроек сервера
⚠️ Откат к предыдущей версии
⚠️ Массовое удаление данных
⚠️ Изменение прав доступа на сервере
⚠️ Изменение конфигурации nginx

### Что категорически запрещено

❌ Удалять production базу данных
❌ Изменять SSH ключи на сервере
❌ Останавливать критические сервисы (nginx, firewall)
❌ Делиться паролями или ключами
❌ Деплоить непротестированный код
❌ Игнорировать ошибки компиляции
❌ Коммитить конфиденциальные данные (пароли, ключи API)

### Безопасность

🔒 **Никогда не храните пароли в коде**

```dart
// ❌ ПЛОХО
final password = "GoGoMarket2025!Secure";

// ✅ ХОРОШО
final password = dotenv.env['DB_PASSWORD'];
```

🔒 **Используйте environment variables**

```dart
// .env файл (не коммитить в git!)
API_KEY=your_api_key_here
DB_PASSWORD=your_password_here

// Использование
import 'package:flutter_dotenv/flutter_dotenv.dart';

final apiKey = dotenv.env['API_KEY'];
```

🔒 **Проверяйте input пользователя**

```dart
// Всегда валидируйте данные от пользователя
if (userInput.isEmpty || userInput.length > 100) {
  throw ValidationException('Некорректный ввод');
}

// Экранируйте данные перед отправкой на сервер
final sanitized = sanitizeInput(userInput);
```

---

## 🎓 Дополнительные рекомендации

### Лучшие практики

1. **Всегда тестируйте локально перед деплоем**
2. **Пишите понятные commit messages**
3. **Документируйте сложную логику комментариями**
4. **Используйте meaningful имена переменных**
5. **Следуйте Flutter/Dart style guide**
6. **Добавляйте logging для отладки production**
7. **Обрабатывайте все возможные ошибки**
8. **Показывайте feedback пользователю**

### Полезные команды

```bash
# Проверка статуса git
git status

# Просмотр изменений
git diff

# Просмотр последних коммитов
git log --oneline -10

# Проверка Flutter
flutter doctor

# Анализ кода Flutter
flutter analyze

# Форматирование кода
dart format lib/

# Запуск тестов
flutter test

# Очистка кэша
flutter clean

# Обновление зависимостей
flutter pub get

# Проверка деплоя
curl -I https://165.232.81.31
```

---

## 🤝 Коммуникация с пользователем

### Формат отчетов

Всегда сообщайте о своих действиях в структурированном формате:

```
🤖 [АНАЛИЗ]
   Обнаружена проблема в файле X
   Причина: описание причины

🤖 [ИСПРАВЛЕНИЕ]
   Внесены следующие изменения:
   1. Изменение 1
   2. Изменение 2

🤖 [ТЕСТИРОВАНИЕ]
   ✅ Тест 1 пройден
   ✅ Тест 2 пройден

🤖 [ДЕПЛОЙ]
   ✅ Деплой завершен успешно
   🔗 https://165.232.81.31

🤖 [РЕКОМЕНДАЦИИ]
   Рекомендую дополнительно проверить: [список]
```

### При возникновении проблем

Если вы столкнулись с проблемой, которую не можете решить:

```
🤖: "❌ Обнаружена проблема, которая требует вашего внимания:

    Проблема: [описание]
    
    Что я пробовал:
    1. Действие 1 - не помогло
    2. Действие 2 - не помогло
    
    Возможные решения:
    1. Вариант 1
    2. Вариант 2
    
    Как бы вы хотели продолжить?"
```

---

## ✅ Финальный чек-лист

Перед завершением работы убедитесь, что:

- [ ] Все ошибки исправлены и протестированы
- [ ] Код прошел локальное тестирование
- [ ] Изменения закоммичены с понятным сообщением
- [ ] Код запушен в репозиторий
- [ ] Деплой выполнен успешно
- [ ] Сайт доступен и работает
- [ ] Исправленный функционал проверен на production
- [ ] Пользователь проинформирован о результатах

---

**Помните:** Вы работаете с production сервером реального приложения. Будьте внимательны, тестируйте изменения и всегда имейте план отката на случай проблем.

**Удачи! 🚀**
