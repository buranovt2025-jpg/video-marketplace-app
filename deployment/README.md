# 🚀 Система автоматизации деплоя GoGoMarket

Полная система автоматического деплоя Flutter web приложения GoGoMarket на Digital Ocean с интеграцией для Cursor AI.

---

## 📋 Содержание

- [Обзор](#обзор)
- [Быстрый старт](#быстрый-старт)
- [Установка и настройка](#установка-и-настройка)
- [Использование](#использование)
- [Структура проекта](#структура-проекта)
- [Конфигурация](#конфигурация)
- [Troubleshooting](#troubleshooting)
- [FAQ](#faq)

---

## 🎯 Обзор

Эта система автоматизации предоставляет:

✅ **Автоматический деплой** Flutter web приложения
✅ **Проверка изменений** перед деплоем
✅ **Резервное копирование** текущей версии
✅ **Проверка деплоя** после завершения
✅ **Детальное логирование** всех операций
✅ **Интеграция с Cursor AI** для автоматических исправлений
✅ **GitHub Actions workflow** для CI/CD
✅ **Обработка ошибок** на каждом этапе

### Технологии

- **Flutter Web** - фронтенд приложение
- **Digital Ocean** - хостинг
- **Nginx** - веб-сервер
- **Bash Scripts** - автоматизация
- **Git** - контроль версий
- **SSH** - безопасное подключение
- **GitHub Actions** - CI/CD (опционально)

---

## ⚡ Быстрый старт

### Предварительные требования

- ✅ Flutter SDK установлен
- ✅ Git установлен
- ✅ SSH ключи настроены (см. [SSH_SETUP.md](./SSH_SETUP.md))
- ✅ Доступ к серверу Digital Ocean

### Шаги для первого деплоя

```bash
# 1. Перейдите в директорию
cd ~/cursor_deploy_automation

# 2. Настройте конфигурацию
nano deploy_config.sh
# Измените PROJECT_DIR на путь к вашему проекту

# 3. Настройте SSH (если еще не настроено)
cat SSH_SETUP.md

# 4. Запустите деплой
./auto_deploy.sh

# 5. Проверьте результат
./check_deployment.sh
```

---

## 🔧 Установка и настройка

### Шаг 1: Клонирование проекта

```bash
# Система уже установлена в ~/cursor_deploy_automation/
cd ~/cursor_deploy_automation
ls -la
```

### Шаг 2: Настройка SSH

**Детальная инструкция:** [SSH_SETUP.md](./SSH_SETUP.md)

```bash
# Создайте SSH ключ (если нет)
ssh-keygen -t ed25519 -C "your_email@example.com"

# Добавьте ключ на сервер
ssh-copy-id root@165.232.81.31

# Протестируйте подключение
ssh root@165.232.81.31 "echo 'SSH работает'"
```

### Шаг 3: Настройка конфигурации

Откройте `deploy_config.sh` и настройте параметры:

```bash
nano deploy_config.sh
```

**Обязательные параметры для изменения:**

```bash
# Путь к вашему Flutter проекту
export PROJECT_DIR="/path/to/your/video-marketplace-app"

# IP адрес вашего сервера (уже настроен)
export SERVER_IP="165.232.81.31"

# Ветка для деплоя (обычно main)
export DEPLOY_BRANCH="main"
```

### Шаг 4: Установка прав выполнения

```bash
chmod +x auto_deploy.sh
chmod +x check_deployment.sh
```

### Шаг 5: Первый тестовый запуск

```bash
# Запустите с флагом --force для принудительного деплоя
./auto_deploy.sh --force
```

---

## 💻 Использование

### Автоматический деплой

**Основная команда:**

```bash
cd ~/cursor_deploy_automation
./auto_deploy.sh
```

**Опции:**

```bash
# Принудительный деплой (даже без новых изменений)
./auto_deploy.sh --force
./auto_deploy.sh -f

# Просмотр логов последнего деплоя
tail -f logs/deploy_*.log

# Просмотр всех логов
ls -lt logs/
```

### Проверка деплоя

```bash
# Запустите скрипт проверки
./check_deployment.sh
```

Скрипт проверит:

- ✅ Доступность сервера (ping, SSH)
- ✅ Статус Nginx
- ✅ Наличие файлов приложения
- ✅ Доступность сайта (HTTP/HTTPS)
- ✅ Логи Nginx
- ✅ Информацию о сервере

### Работа с Cursor AI

**Для Cursor Composer/Agent:**

1. Откройте проект в Cursor
2. Прочитайте [CURSOR_INSTRUCTIONS.md](./CURSOR_INSTRUCTIONS.md)
3. Попросите Cursor исправить ошибки:

```
"Исправь проблему с созданием заказов и задеплой на сервер"
```

Cursor автоматически:
- Проанализирует код
- Исправит ошибки
- Протестирует локально
- Закоммитит изменения
- Запустит деплой
- Проверит результат

### Просмотр логов

```bash
# Последний лог деплоя
tail -f ~/cursor_deploy_automation/logs/deploy_*.log | tail -n 100

# Все логи деплоя
ls -lt ~/cursor_deploy_automation/logs/

# Логи nginx на сервере
ssh gogomarket "tail -f /var/log/nginx/error.log"
```

### Резервные копии

```bash
# Просмотр доступных резервных копий
ls -lt ~/cursor_deploy_automation/backups/

# Информация о размере резервных копий
du -sh ~/cursor_deploy_automation/backups/*
```

### Откат к предыдущей версии

Если деплой привел к проблемам:

```bash
# 1. Найдите нужную резервную копию
ls -lt ~/cursor_deploy_automation/backups/

# 2. Подключитесь к серверу
ssh gogomarket

# 3. Перейдите в веб-директорию
cd /var/www/html

# 4. Удалите текущие файлы
sudo rm -rf *

# 5. Восстановите из резервной копии (замените DATE_TIME)
sudo tar -xzf ~/backups/backup_DATE_TIME.tar.gz

# 6. Перезапустите nginx
sudo systemctl restart nginx
```

---

## 📁 Структура проекта

```
~/cursor_deploy_automation/
├── auto_deploy.sh              # Главный скрипт деплоя
├── check_deployment.sh         # Скрипт проверки деплоя
├── deploy_config.sh            # Конфигурационный файл
├── SSH_SETUP.md                # Инструкция по настройке SSH
├── CURSOR_INSTRUCTIONS.md      # Инструкции для Cursor AI
├── README.md                   # Этот файл
├── .github/
│   └── workflows/
│       └── deploy.yml          # GitHub Actions workflow
├── logs/                       # Директория с логами
│   ├── deploy_20251231_120000.log
│   └── ...
└── backups/                    # Резервные копии
    ├── backup_20251231_120000.tar.gz
    └── ...
```

---

## ⚙️ Конфигурация

### Основные параметры (deploy_config.sh)

#### Настройки сервера

```bash
export SERVER_IP="165.232.81.31"          # IP сервера
export SERVER_USER="root"                 # Пользователь
export SSH_PORT="22"                      # SSH порт
export SERVER_WEB_DIR="/var/www/html"    # Директория веб-приложения
```

#### Настройки проекта

```bash
export PROJECT_DIR="/home/ubuntu/video-marketplace-app"  # Путь к проекту
export BUILD_DIR="${PROJECT_DIR}/build/web"              # Путь к билду
export DEPLOY_BRANCH="main"                              # Ветка для деплоя
```

#### Настройки Flutter Build

```bash
export FLUTTER_BUILD_CMD="flutter build web --release"  # Команда билда
export FLUTTER_BUILD_FLAGS="--web-renderer canvaskit"   # Флаги
export CLEAN_BEFORE_BUILD="true"                        # Очистка перед билдом
```

#### Настройки логирования

```bash
export LOG_DIR="${HOME}/cursor_deploy_automation/logs"  # Директория логов
export MAX_LOGS="10"                                     # Макс. кол-во логов
```

#### Настройки резервного копирования

```bash
export CREATE_BACKUP="true"                                  # Создавать бэкап
export BACKUP_DIR="${HOME}/cursor_deploy_automation/backups" # Директория
export MAX_BACKUPS="5"                                       # Макс. кол-во
```

#### Дополнительные настройки

```bash
export CHECK_CHANGES="true"     # Проверять изменения перед деплоем
export RUN_TESTS="false"        # Запускать тесты (flutter test)
export RESTART_NGINX="true"     # Перезапускать nginx
```

---

## 🔧 Troubleshooting

### Проблема 1: «Flutter не найден»

**Ошибка:**
```
❌ Flutter не установлен!
```

**Решение:**

```bash
# Проверьте установку Flutter
which flutter
flutter --version

# Если Flutter не установлен, установите его
# Следуйте инструкциям: https://docs.flutter.dev/get-started/install
```

### Проблема 2: «SSH подключение не удалось»

**Ошибка:**
```
❌ Не удалось подключиться к серверу!
```

**Решение:**

```bash
# Проверьте SSH ключ
ssh-add -l

# Добавьте ключ, если отсутствует
ssh-add ~/.ssh/id_ed25519

# Протестируйте подключение
ssh root@165.232.81.31 "echo 'test'"

# Если не работает, смотрите SSH_SETUP.md
```

### Проблема 3: «Permission denied»

**Ошибка:**
```
bash: ./auto_deploy.sh: Permission denied
```

**Решение:**

```bash
# Установите права выполнения
chmod +x auto_deploy.sh
chmod +x check_deployment.sh
```

### Проблема 4: «Flutter build failed»

**Ошибка:**
```
❌ Ошибка при сборке приложения!
```

**Решение:**

```bash
# Перейдите в директорию проекта
cd /path/to/video-marketplace-app

# Очистите кэш
flutter clean

# Обновите зависимости
flutter pub get

# Попробуйте собрать вручную
flutter build web --release --web-renderer canvaskit

# Проверьте ошибки
flutter analyze
```

### Проблема 5: «Nginx не запускается»

**Ошибка:**
```
❌ Ошибка при перезапуске nginx!
```

**Решение:**

```bash
# Подключитесь к серверу
ssh gogomarket

# Проверьте конфигурацию
sudo nginx -t

# Просмотрите логи
sudo tail -f /var/log/nginx/error.log

# Перезапустите вручную
sudo systemctl restart nginx

# Проверьте статус
sudo systemctl status nginx
```

### Проблема 6: «Сайт возвращает 404/500»

**Решение:**

```bash
# Проверьте наличие файлов на сервере
ssh gogomarket "ls -la /var/www/html"

# Проверьте права доступа
ssh gogomarket "ls -ld /var/www/html"

# Исправьте права, если необходимо
ssh gogomarket "sudo chmod -R 755 /var/www/html"

# Проверьте index.html
ssh gogomarket "test -f /var/www/html/index.html && echo 'Exists' || echo 'Missing'"
```

### Проблема 7: «Изменения не применились»

**Решение:**

```bash
# 1. Проверьте, что изменения закоммичены
cd /path/to/video-marketplace-app
git status

# 2. Проверьте, что изменения запушены
git log origin/main --oneline -5

# 3. Очистите кэш браузера (Ctrl+Shift+R)

# 4. Проверьте версию файлов на сервере
ssh gogomarket "ls -lt /var/www/html | head -10"

# 5. Принудительный деплой
./auto_deploy.sh --force
```

---

## ❓ FAQ

### Как часто можно запускать деплой?

Можно запускать деплой сколько угодно раз. Скрипт проверяет наличие новых изменений и пропустит деплой, если изменений нет (кроме случая с флагом `--force`).

### Сколько времени занимает деплой?

Обычный деплой занимает **3-5 минут**:
- Сборка Flutter: 2-3 минуты
- Копирование файлов: 30 секунд
- Перезапуск nginx: 5 секунд

### Что делать, если деплой завис?

Нажмите `Ctrl+C` для прерывания. Затем:

```bash
# Проверьте процессы Flutter
pkill -f flutter

# Попробуйте снова
./auto_deploy.sh
```

### Можно ли деплоить из другой ветки?

Да, измените в `deploy_config.sh`:

```bash
export DEPLOY_BRANCH="develop"  # или другая ветка
```

### Как настроить уведомления о деплое?

В `deploy_config.sh` добавьте:

```bash
export NOTIFICATION_EMAIL="your@email.com"
# или
export WEBHOOK_URL="https://hooks.slack.com/services/..."
```

### Как деплоить на другой сервер?

Измените в `deploy_config.sh`:

```bash
export SERVER_IP="новый_ip_адрес"
export SERVER_USER="новый_пользователь"
```

И настройте SSH ключ для нового сервера.

### Безопасно ли хранить пароли в скриптах?

❌ **НЕТ!** Пароли **НЕ должны** храниться в скриптах.

Используйте SSH ключи для аутентификации. См. [SSH_SETUP.md](./SSH_SETUP.md).

### Можно ли откатить деплой?

Да, см. раздел [Откат к предыдущей версии](#откат-к-предыдущей-версии).

### Как работает GitHub Actions workflow?

GitHub Actions автоматически запускает деплой при push в указанную ветку. Для настройки:

1. Добавьте SSH ключ в GitHub Secrets (название: `SSH_PRIVATE_KEY`)
2. Добавьте IP сервера в Secrets (название: `SERVER_IP`)
3. Настройте workflow в `.github/workflows/deploy.yml`

Подробнее см. комментарии в `deploy.yml`.

---

## 📚 Дополнительные ресурсы

- [SSH_SETUP.md](./SSH_SETUP.md) - Настройка SSH
- [CURSOR_INSTRUCTIONS.md](./CURSOR_INSTRUCTIONS.md) - Инструкции для Cursor AI
- [Flutter Documentation](https://docs.flutter.dev/) - Документация Flutter
- [Digital Ocean Docs](https://docs.digitalocean.com/) - Документация Digital Ocean
- [Nginx Documentation](https://nginx.org/en/docs/) - Документация Nginx

---

## 📝 Changelog

### Version 1.0.0 (2025-01-01)

- ✅ Первый релиз
- ✅ Автоматический деплой
- ✅ Резервное копирование
- ✅ Проверка деплоя
- ✅ Интеграция с Cursor AI
- ✅ GitHub Actions workflow
- ✅ Детальная документация

---

## 🤝 Поддержка

Если у вас возникли проблемы:

1. Проверьте [Troubleshooting](#troubleshooting)
2. Проверьте [FAQ](#faq)
3. Просмотрите логи деплоя
4. Проверьте логи nginx на сервере

---

## 📄 Лицензия

Этот проект создан для внутреннего использования проекта GoGoMarket.

---

**Создано с ❤️ для GoGoMarket Team**

**Приятного деплоя! 🚀**
