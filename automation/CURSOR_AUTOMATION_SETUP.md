# 🚀 CURSOR AUTOMATION - ПОЛНОЕ РУКОВОДСТВО ПО НАСТРОЙКЕ

**Версия:** 1.0  
**Дата:** 31 декабря 2025  
**Проект:** GoGoMarket (Flutter Web Application)

---

## 📋 Содержание

1. [Обзор системы](#обзор-системы)
2. [Предварительные требования](#предварительные-требования)
3. [Установка и настройка](#установка-и-настройка)
4. [Настройка GitHub Secrets](#настройка-github-secrets)
5. [Настройка .cursorrules в проекте](#настройка-cursorrules-в-проекте)
6. [Настройка Cursor Composer](#настройка-cursor-composer)
7. [Тестирование системы](#тестирование-системы)
8. [Ежедневное использование](#ежедневное-использование)
9. [Troubleshooting](#troubleshooting)
10. [Меры предосторожности](#меры-предосторожности)
11. [Как отключить автоматизацию](#как-отключить-автоматизацию)

---

## 🎯 Обзор системы

### Что делает эта система?

Система автоматизации Cursor обеспечивает **полностью автономный workflow** для разработки:

```
Cursor AI исправляет код
         ↓
Автоматический коммит (conventional commits)
         ↓
Автоматический push в GitHub
         ↓
GitHub Actions запускает CI/CD
         ↓
Автоматический деплой на Digital Ocean
         ↓
✅ Приложение обновлено и работает!
```

### Компоненты системы

1. **`.cursorrules`** - Правила для Cursor AI
2. **`auto_commit_push.sh`** - Автоматический коммит и пуш
3. **`automation_config.sh`** - Конфигурация системы
4. **`watch_and_deploy.sh`** - Мониторинг файлов (опционально)
5. **`.github/workflows/auto_deploy.yml`** - GitHub Actions для деплоя
6. **Интеграция с `~/cursor_deploy_automation/`** - Существующая система деплоя

---

## ✅ Предварительные требования

### На вашей машине

- [x] **Git** установлен и настроен
- [x] **SSH ключ** для GitHub настроен
- [x] **Cursor IDE** установлен
- [x] **Доступ к репозиторию** `buranovt2025-jpg/video-marketplace-app`

### На сервере (Digital Ocean)

- [x] **SSH доступ** к серверу 165.232.81.31
- [x] **Nginx** установлен и настроен
- [x] **Директория** `/var/www/gogomarket` существует

### В GitHub

- [x] **Права администратора** на репозиторий
- [x] **GitHub Actions** включены
- [x] Возможность добавлять **Secrets**

---

## 🛠 Установка и настройка

### Шаг 1: Установка файлов системы

Файлы уже созданы в `~/cursor_automation/`. Проверьте их наличие:

```bash
ls -la ~/cursor_automation/
```

Должны быть следующие файлы:
```
~/cursor_automation/
├── .cursorrules                    # Правила для Cursor AI
├── .github/
│   └── workflows/
│       └── auto_deploy.yml         # GitHub Actions workflow
├── auto_commit_push.sh             # Скрипт автокоммита
├── automation_config.sh            # Конфигурация
├── watch_and_deploy.sh             # Мониторинг файлов
├── CURSOR_AUTOMATION_SETUP.md      # Эта инструкция
├── README.md                       # Документация
├── logs/                           # Директория для логов
└── backups/                        # Директория для backup'ов
```

### Шаг 2: Установка прав на выполнение скриптов

```bash
chmod +x ~/cursor_automation/*.sh
```

Проверка:
```bash
ls -lh ~/cursor_automation/*.sh
```

### Шаг 3: Настройка конфигурации

Отредактируйте `~/cursor_automation/automation_config.sh`:

```bash
nano ~/cursor_automation/automation_config.sh
```

**Важные параметры для проверки:**

```bash
# Путь к вашему проекту
export PROJECT_PATH="/home/ubuntu/video-marketplace-app"

# Если путь другой - измените его!
# Проверьте: ls -la /home/ubuntu/video-marketplace-app
```

**Другие настройки (можно оставить по умолчанию):**
- `MIN_COMMIT_INTERVAL=60` - минимальное время между коммитами (секунды)
- `AUTO_PUSH=true` - автоматический push
- `AUTO_ADD_ALL=true` - добавлять все изменения
- `CHECK_SECRETS=true` - проверять секреты перед коммитом

### Шаг 4: Тестовый запуск

Проверьте что система работает:

```bash
# Перейдите в директорию проекта
cd /home/ubuntu/video-marketplace-app  # или ваш путь

# Создайте тестовое изменение
echo "# Test" >> README.md

# Запустите скрипт автокоммита
~/cursor_automation/auto_commit_push.sh --dry-run
```

Должен показать:
- Какие файлы изменены
- Какой тип коммита будет создан
- Какой commit message будет использован

---

## 🔐 Настройка GitHub Secrets

### Шаг 1: Создание SSH ключа для деплоя (если еще нет)

```bash
# Создать новый SSH ключ специально для GitHub Actions
ssh-keygen -t ed25519 -C "github-actions@gogomarket" -f ~/.ssh/github_deploy_key -N ""

# Добавить публичный ключ на сервер
ssh-copy-id -i ~/.ssh/github_deploy_key.pub root@165.232.81.31
```

### Шаг 2: Добавление SSH_PRIVATE_KEY в GitHub Secrets

1. **Скопируйте приватный ключ:**
   ```bash
   cat ~/.ssh/github_deploy_key
   ```
   
2. **Откройте GitHub:**
   - Перейдите в репозиторий: https://github.com/buranovt2025-jpg/video-marketplace-app
   - Settings → Secrets and variables → Actions
   
3. **Создайте новый Secret:**
   - Нажмите "New repository secret"
   - Name: `SSH_PRIVATE_KEY`
   - Value: Вставьте содержимое приватного ключа (весь текст от `-----BEGIN` до `-----END`)
   - Нажмите "Add secret"

### Шаг 3: Проверка других необходимых Secrets

GitHub Actions workflow использует следующие переменные окружения (они жестко закодированы в workflow, но вы можете переместить их в Secrets для большей безопасности):

- `DEPLOY_SERVER`: 165.232.81.31
- `DEPLOY_USER`: root
- `DEPLOY_PATH`: /var/www/gogomarket

**Опционально:** Если хотите сделать их секретными, создайте Secrets с этими именами.

---

## 📝 Настройка .cursorrules в проекте

### Шаг 1: Копирование .cursorrules в корень проекта

```bash
# Скопировать .cursorrules в корень вашего проекта
cp ~/cursor_automation/.cursorrules /home/ubuntu/video-marketplace-app/.cursorrules

# Проверить
ls -la /home/ubuntu/video-marketplace-app/.cursorrules
```

### Шаг 2: Копирование GitHub Actions workflow

```bash
# Создать директорию если не существует
mkdir -p /home/ubuntu/video-marketplace-app/.github/workflows

# Скопировать workflow
cp ~/cursor_automation/.github/workflows/auto_deploy.yml \
   /home/ubuntu/video-marketplace-app/.github/workflows/auto_deploy.yml

# Проверить
ls -la /home/ubuntu/video-marketplace-app/.github/workflows/
```

### Шаг 3: Коммит файлов автоматизации

```bash
cd /home/ubuntu/video-marketplace-app

git add .cursorrules .github/workflows/auto_deploy.yml
git commit -m "ci: добавлена система автоматизации Cursor"
git push
```

---

## 🎨 Настройка Cursor Composer

### Шаг 1: Открыть проект в Cursor

1. Запустите Cursor IDE
2. File → Open Folder
3. Выберите `/home/ubuntu/video-marketplace-app`

### Шаг 2: Активировать Cursor Rules

Cursor автоматически обнаружит `.cursorrules` файл в корне проекта.

**Проверка:**
1. Откройте Cursor Composer (Cmd/Ctrl + I)
2. В правом верхнем углу должна быть иконка ⚙️ (Settings)
3. Убедитесь что "Use .cursorrules" включено

### Шаг 3: Настройка автономного режима

В Cursor Composer:

1. **Включите режим Agent:**
   - Settings → Agent Mode → Enable
   
2. **Настройте автоматические действия:**
   - Settings → Auto-execute commands → Enable
   - Settings → Auto-commit → Enable (если есть такая опция)

3. **Настройте доступ к терминалу:**
   - Settings → Allow terminal access → Enable
   - Это позволит Cursor запускать `auto_commit_push.sh`

### Шаг 4: Тестовое исправление

Попросите Cursor Composer исправить что-то простое:

```
@Composer: Пожалуйста, добавь комментарий в lib/main.dart 
объясняющий что делает функция main(). 
После исправления запусти автоматический коммит используя 
~/cursor_automation/auto_commit_push.sh
```

Cursor должен:
1. ✅ Добавить комментарий
2. ✅ Запустить `auto_commit_push.sh`
3. ✅ Создать коммит с правильным форматом
4. ✅ Запушить в GitHub

---

## 🧪 Тестирование системы

### Тест 1: Ручной коммит

```bash
cd /home/ubuntu/video-marketplace-app

# Создать тестовое изменение
echo "// Test comment" >> lib/main.dart

# Запустить скрипт
~/cursor_automation/auto_commit_push.sh

# Проверить что коммит создан
git log -1
```

**Ожидаемый результат:**
- ✅ Коммит создан с правильным форматом (например: `fix(app): ...`)
- ✅ Push выполнен успешно
- ✅ Лог записан в `~/cursor_automation/logs/automation.log`

### Тест 2: GitHub Actions

1. **Проверьте что workflow запустился:**
   - Перейдите на https://github.com/buranovt2025-jpg/video-marketplace-app/actions
   - Должен быть новый workflow run

2. **Дождитесь завершения:**
   - Code Check → Build → Deploy

3. **Проверьте деплой:**
   ```bash
   curl -I http://165.232.81.31
   ```
   Должен вернуть `HTTP/1.1 200 OK`

### Тест 3: Cursor AI автокоммит

1. Откройте Cursor Composer
2. Попросите сделать простое изменение:
   ```
   Добавь пустую строку в конец файла lib/main.dart
   и автоматически закоммить используя ~/cursor_automation/auto_commit_push.sh
   ```
3. Проверьте в GitHub что коммит появился

### Тест 4: Watch mode (опционально)

```bash
# Запустить watch в фоне
~/cursor_automation/watch_and_deploy.sh --daemon

# Проверить статус
~/cursor_automation/watch_and_deploy.sh --status

# Создать изменение
echo "// Auto-commit test" >> lib/main.dart

# Подождать 5 секунд - автокоммит должен произойти

# Остановить watch
~/cursor_automation/watch_and_deploy.sh --stop
```

---

## 📱 Ежедневное использование

### Обычный рабочий процесс

1. **Откройте Cursor IDE**
   ```bash
   cursor /home/ubuntu/video-marketplace-app
   ```

2. **Работайте с Cursor Composer:**
   - Cursor автоматически использует `.cursorrules`
   - После каждого исправления/улучшения Cursor может автоматически коммитить
   - Или вы можете явно попросить: "закоммить изменения"

3. **Cursor AI делает:**
   - Анализирует код
   - Исправляет ошибки
   - Запускает `auto_commit_push.sh`
   - Push в GitHub
   - GitHub Actions автоматически деплоит

4. **Вы только наблюдаете:**
   - Смотрите логи: `tail -f ~/cursor_automation/logs/automation.log`
   - Проверяете коммиты: https://github.com/buranovt2025-jpg/video-marketplace-app/commits
   - Проверяете деплой: https://github.com/buranovt2025-jpg/video-marketplace-app/actions

### Полезные команды

```bash
# Проверить последние коммиты
git log --oneline -10

# Посмотреть логи автоматизации
tail -f ~/cursor_automation/logs/automation.log

# Посмотреть только коммиты
tail -f ~/cursor_automation/logs/commits.log

# Посмотреть ошибки
tail -f ~/cursor_automation/logs/errors.log

# Ручной коммит если нужно
~/cursor_automation/auto_commit_push.sh

# Dry-run (посмотреть что будет сделано)
~/cursor_automation/auto_commit_push.sh --dry-run

# Отключить автопуш для одного коммита
~/cursor_automation/auto_commit_push.sh --no-push
```

---

## 🔧 Troubleshooting

### Проблема: Скрипт не запускается

**Симптомы:**
```
bash: ~/cursor_automation/auto_commit_push.sh: Permission denied
```

**Решение:**
```bash
chmod +x ~/cursor_automation/auto_commit_push.sh
```

### Проблема: Нет изменений для коммита

**Симптомы:**
```
[INFO] Нет изменений для коммита
```

**Решение:**
- Это нормально - значит нет изменений
- Убедитесь что вы внесли изменения в файлы
- Проверьте: `git status`

### Проблема: Push не работает

**Симптомы:**
```
[ERROR] Не удалось выполнить push
```

**Возможные причины и решения:**

1. **Нет прав на push:**
   ```bash
   # Проверить remote
   git remote -v
   
   # Проверить что SSH ключ добавлен в GitHub
   ssh -T git@github.com
   ```

2. **Конфликты с remote:**
   ```bash
   # Попробовать pull
   git pull --rebase
   
   # Решить конфликты
   # Затем снова push
   ```

3. **Network issues:**
   ```bash
   # Проверить интернет
   ping github.com
   
   # Попробовать позже
   ```

### Проблема: GitHub Actions не запускается

**Проверьте:**

1. **Actions включены:**
   - Settings → Actions → General → "Allow all actions and reusable workflows"

2. **Workflow файл на месте:**
   ```bash
   ls -la .github/workflows/auto_deploy.yml
   ```

3. **SSH_PRIVATE_KEY secret добавлен:**
   - Settings → Secrets → SSH_PRIVATE_KEY должен существовать

4. **Workflow syntax правильный:**
   - GitHub → Actions → может показать ошибки парсинга

### Проблема: Деплой не проходит

**Проверьте логи GitHub Actions:**

1. Перейдите в Actions tab
2. Откройте последний run
3. Посмотрите где именно ошибка

**Частые проблемы:**

- **SSH connection failed:** Проверьте SSH ключ, добавьте его на сервер
- **Permission denied:** Проверьте права на `/var/www/gogomarket` на сервере
- **Nginx не перезагружается:** Проверьте конфигурацию Nginx

### Проблема: Cursor не использует .cursorrules

**Решение:**

1. Убедитесь что файл `.cursorrules` в корне проекта
2. Перезапустите Cursor IDE
3. Проверьте в настройках Cursor что .cursorrules включены
4. Попробуйте явно упомянуть в Composer: "используй правила из .cursorrules"

---

## ⚠️ Меры предосторожности

### 🛡️ Безопасность

1. **НЕ коммитьте секреты:**
   - `.env` файлы должны быть в `.gitignore`
   - Скрипт проверяет наличие секретов, но будьте осторожны

2. **Проверяйте коммиты:**
   - Периодически просматривайте что AI закоммитил
   - Используйте `git log` и GitHub interface

3. **Используйте ветки:**
   - Для экспериментов создавайте feature ветки
   - main ветка автоматически деплоится на production!

### 🚦 Контроль качества

1. **Не отключайте проверки:**
   ```bash
   # В automation_config.sh оставьте включенными:
   CHECK_SECRETS=true
   CHECK_CONFLICTS=true
   ```

2. **Используйте dry-run:**
   ```bash
   ~/cursor_automation/auto_commit_push.sh --dry-run
   ```

3. **Смотрите логи:**
   ```bash
   tail -f ~/cursor_automation/logs/automation.log
   ```

### 🎯 Лимиты

1. **Не слишком частые коммиты:**
   - MIN_COMMIT_INTERVAL=60 (секунд между коммитами)
   - Можно увеличить если нужно

2. **Не слишком большие коммиты:**
   - MAX_FILES_PER_COMMIT=20
   - Большие изменения лучше разбить

---

## 🔴 Как отключить автоматизацию

### Временное отключение

**Отключить автоматический push:**
```bash
nano ~/cursor_automation/automation_config.sh

# Изменить:
AUTO_PUSH=false
```

**Отключить автокоммит:**
```bash
# В automation_config.sh:
AUTO_COMMIT_ENABLED=false
```

**Отключить всю систему:**
```bash
# В automation_config.sh:
AUTOMATION_ENABLED=false
```

### Полное отключение

1. **Удалить .cursorrules из проекта:**
   ```bash
   rm /home/ubuntu/video-marketplace-app/.cursorrules
   ```

2. **Отключить GitHub Actions workflow:**
   ```bash
   # Переименовать файл
   mv .github/workflows/auto_deploy.yml \
      .github/workflows/auto_deploy.yml.disabled
   ```

3. **Остановить watch daemon если запущен:**
   ```bash
   ~/cursor_automation/watch_and_deploy.sh --stop
   ```

---

## 📞 Поддержка

### Где искать помощь

1. **Логи:**
   ```bash
   ~/cursor_automation/logs/automation.log
   ~/cursor_automation/logs/errors.log
   ```

2. **GitHub Actions:**
   - https://github.com/buranovt2025-jpg/video-marketplace-app/actions

3. **Документация:**
   - `~/cursor_automation/README.md`
   - Эта инструкция

### Полезные ссылки

- **Репозиторий:** https://github.com/buranovt2025-jpg/video-marketplace-app
- **Сервер:** http://165.232.81.31
- **Conventional Commits:** https://www.conventionalcommits.org/

---

## ✅ Чеклист успешной настройки

- [ ] Все файлы скопированы в `~/cursor_automation/`
- [ ] Права на выполнение установлены (`chmod +x`)
- [ ] `automation_config.sh` настроен с правильным PROJECT_PATH
- [ ] `.cursorrules` скопирован в корень проекта
- [ ] GitHub Actions workflow скопирован в `.github/workflows/`
- [ ] SSH_PRIVATE_KEY добавлен в GitHub Secrets
- [ ] Тестовый коммит прошел успешно
- [ ] GitHub Actions workflow запускается
- [ ] Деплой на сервер работает
- [ ] Cursor Composer использует .cursorrules
- [ ] Автокоммиты работают

---

**🎉 Поздравляем! Система полностью настроена и готова к работе!**

Теперь Cursor AI может работать полностью автономно:
- Исправляет ошибки
- Автоматически коммитит
- Автоматически пушит
- GitHub Actions автоматически деплоит

**Приятной работы! 🚀**
