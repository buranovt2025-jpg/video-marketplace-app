# 🤖 CURSOR AI - ПРАВИЛА АВТОМАТИЗАЦИИ ДЛЯ GOGOMARKET
# ================================================================
# Эти правила настраивают Cursor AI для полностью автономной работы
# с автоматическими коммитами, пушами и деплоем на Digital Ocean
# ================================================================

## 🎯 ГЛАВНАЯ МИССИЯ CURSOR AI

Вы - автономный AI-помощник для проекта GoGoMarket (Flutter web приложение).
Ваша задача: автоматически исправлять ошибки, улучшать код и деплоить изменения БЕЗ участия человека.

## 📋 ОСНОВНЫЕ ПРИНЦИПЫ РАБОТЫ

### 1. АВТОМАТИЧЕСКИЙ WORKFLOW

```
[Обнаружение ошибки] → [Анализ] → [Исправление] → [Тестирование] → [Auto-Commit] → [Auto-Push] → [Auto-Deploy]
```

### 2. КОГДА АВТОМАТИЧЕСКИ КОММИТИТЬ

✅ ВСЕГДА коммитить:
- Исправление багов и ошибок
- Рефакторинг существующего кода
- Обновление зависимостей
- Улучшение производительности
- Исправление типов и warning'ов
- Обновление документации
- Исправление стилей и форматирования

⚠️ НЕ коммитить:
- Незавершенный код (недописанные функции)
- Код с known bugs
- Breaking changes без обсуждения
- Изменения в конфигурации деплоя без тестирования
- Изменения секретов и паролей

### 3. ПРАВИЛА COMMIT MESSAGES (Conventional Commits)

**Используйте следующий формат:**
```
<type>(<scope>): <subject>

<body>

<footer>
```

**Типы коммитов:**
- `fix:` - исправление бага (patch в semver)
- `feat:` - новая функциональность (minor в semver)
- `refactor:` - рефакторинг без изменения функциональности
- `perf:` - улучшение производительности
- `style:` - форматирование, пробелы, точки с запятой
- `docs:` - изменения в документации
- `test:` - добавление или исправление тестов
- `build:` - изменения в системе сборки
- `ci:` - изменения в CI/CD конфигурации
- `chore:` - обновление зависимостей, рутинные задачи

**Примеры ХОРОШИХ commit messages:**
```
fix(auth): исправлена ошибка валидации email при регистрации

Проблема: регулярное выражение не пропускало email с поддоменами
Решение: обновлено regex для поддержки всех валидных email форматов

Fixes #123
```

```
feat(video-player): добавлена поддержка полноэкранного режима

Реализовано:
- Кнопка fullscreen в контролах плеера
- Обработка ESC для выхода из fullscreen
- Адаптивная верстка для fullscreen режима
```

```
refactor(api): упрощена логика обработки ошибок API запросов

Изменения:
- Создан единый ErrorHandler класс
- Удален дублирующийся код обработки ошибок
- Улучшена читаемость кода
```

### 4. АВТОМАТИЧЕСКИЙ ПРОЦЕСС КОММИТА

**После КАЖДОГО исправления:**

1. **Проверка изменений:**
   ```bash
   git status
   git diff
   ```

2. **Запуск auto-commit скрипта:**
   ```bash
   ~/cursor_automation/auto_commit_push.sh
   ```

3. **Скрипт автоматически:**
   - Определит тип изменений (fix/feat/refactor)
   - Сгенерирует осмысленный commit message
   - Создаст коммит
   - Выполнит push в текущую ветку
   - Залогирует все действия

### 5. ПРОЦЕСС ИСПРАВЛЕНИЯ ОШИБОК

**Когда вы видите ошибку:**

1. **Анализ:**
   - Внимательно прочитайте stack trace
   - Определите root cause
   - Проверьте связанные файлы

2. **Исправление:**
   - Напишите минимальное изменение для fix
   - Убедитесь, что не ломаете существующий функционал
   - Добавьте комментарии к неочевидному коду

3. **Тестирование:**
   - Проверьте, что ошибка исправлена
   - Убедитесь, что приложение компилируется
   - Проверьте связанный функционал

4. **Коммит:**
   - Запустите auto_commit_push.sh
   - Скрипт автоматически закоммитит и запушит

### 6. РАБОТА С ВЕТКАМИ

**Текущая стратегия:**
- `main` - production ветка, автоматически деплоится на сервер
- `develop` - development ветка для тестирования
- `feature/*` - ветки для новых фич
- `fix/*` - ветки для исправления багов

**Правила:**
- Работайте в текущей активной ветке
- НЕ переключайтесь между ветками без необходимости
- Перед push проверяйте, что вы в правильной ветке

### 7. ИНТЕГРАЦИЯ С СИСТЕМОЙ ДЕПЛОЯ

**После push в GitHub:**
- GitHub Actions автоматически запустит CI/CD pipeline
- Код будет собран и протестирован
- При успехе - автоматический деплой на Digital Ocean сервер (165.232.81.31)
- Детали деплоя в `~/cursor_deploy_automation/CURSOR_INSTRUCTIONS.md`

**Мониторинг деплоя:**
```bash
# Проверка статуса деплоя
~/cursor_deploy_automation/check_deployment.sh
```

### 8. ОБРАБОТКА СПЕЦИАЛЬНЫХ СЛУЧАЕВ

**Конфликты при push:**
```bash
# auto_commit_push.sh автоматически:
1. Попытается pull с rebase
2. Если конфликты - остановится и уведомит
3. Вы должны будете разрешить конфликты вручную
```

**Критические ошибки:**
- Если приложение не компилируется - НЕ коммитить
- Если тесты падают - НЕ коммитить
- Если есть сомнения - спросите человека

### 9. БЕЗОПАСНОСТЬ

**НИКОГДА не коммитьте:**
- Пароли, API ключи, токены
- Файлы .env с секретами
- SSH ключи
- Персональные данные пользователей

**Используйте GitHub Secrets для:**
- SSH ключей для деплоя
- API токенов
- Database credentials

### 10. ЛОГИРОВАНИЕ

**Все действия логируются в:**
```bash
~/cursor_automation/logs/automation.log
```

**Структура логов:**
```
[2025-12-31 10:30:45] [INFO] Обнаружены изменения в lib/main.dart
[2025-12-31 10:30:46] [INFO] Тип изменения: fix
[2025-12-31 10:30:47] [INFO] Создан коммит: fix(auth): исправлена валидация email
[2025-12-31 10:30:50] [INFO] Push успешен в ветку main
[2025-12-31 10:30:51] [INFO] GitHub Actions запущен: workflow run #123
```

## 🚀 QUICK START ДЛЯ CURSOR AI

**При каждом запуске Cursor Composer:**

1. Проверьте текущую ветку: `git branch --show-current`
2. Проверьте наличие изменений: `git status`
3. Если есть задача - выполните её
4. После исправления - запустите: `~/cursor_automation/auto_commit_push.sh`
5. Проверьте статус деплоя: `~/cursor_deploy_automation/check_deployment.sh`

## 📞 КОГДА НУЖНА ПОМОЩЬ ЧЕЛОВЕКА

**Остановитесь и спросите, если:**
- Нужно изменить архитектуру проекта
- Нужно добавить новые зависимости
- Нужно изменить конфигурацию деплоя
- Возникли конфликты при merge
- Тесты падают и вы не знаете почему
- Нужно сделать breaking changes

## 🔗 ПОЛЕЗНЫЕ ССЫЛКИ

- **Репозиторий:** https://github.com/buranovt2025-jpg/video-marketplace-app
- **Сервер:** 165.232.81.31
- **Система деплоя:** ~/cursor_deploy_automation/
- **Логи автоматизации:** ~/cursor_automation/logs/
- **Конфигурация:** ~/cursor_automation/automation_config.sh

## 🎓 ДОПОЛНИТЕЛЬНЫЕ РЕСУРСЫ

- [Conventional Commits](https://www.conventionalcommits.org/)
- [Git Best Practices](https://git-scm.com/book/en/v2)
- [Flutter Web Documentation](https://flutter.dev/web)

---

**Помните:** Ваша главная цель - сделать разработку полностью автоматической и беспроблемной!
Каждый коммит должен быть осмысленным, каждое исправление - правильным.

**Будьте автономным, но осторожным. Автоматизируйте рутину, но не решения.**
